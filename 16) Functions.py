def func(a,b):
    return a+b

a=int(input("enter first number"))
b=int(input("enter second number"))

print(func(a,b))

# functions can have zero or n number of parameters
# functions can have zero or n number of return statements but only one return statement can be executed at a time.
