# What are default arguments ? 
# default arguments are the arguments that have default values on them , when no value is assigned to them.


# all the times, the default arguments should be at the end 
# if you do not keep them at end , then you will receive an error.
def summ(num1,num2=30):
    print(num1+num2)
    
summ(12)
